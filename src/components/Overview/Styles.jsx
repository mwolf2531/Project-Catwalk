import React from "react";

const Styles = (props) => {
  return (
    <div>
      [This is where bubbles appear]
      {/* TODO: map over styles prop and display individual style inside of circles */}
      {/* TODO: add a check to current style selected */}
    </div>
  );
};

export default Styles;
