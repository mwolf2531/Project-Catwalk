import React from "react";

const Caraselle = (props) => {
  return (
    <div>
      {/* TODO: make column of img boxes for current style (map images) */}
      {/* TODO: add arrow buttons to top and bottom to cyle through pics */}
    </div>
  );
};

export default Caraselle;
